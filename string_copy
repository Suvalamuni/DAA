#include <stdio.h>
#include <string.h>

int main()
{
    char str1[100], str2[100];

    printf("Enter a string: ");
    scanf("%s", str1);

    // Copying str1 to str2 using strcpy() function
    strcpy(str2, str1);

    printf("The copied string is: %s", str2);

    return 0;
}
